package system

import (
	"github.com/TheLazyLemur/tinyecs/ecs"
)

type {{ .SystemName }} struct {
	BaseSystem *ecs.BaseSystem
}

func (s *{{ .SystemName }}) Update(entites []*ecs.Entity, dt float32) {
}

func (s *{{ .SystemName }}) SetBaseSystem(bs *ecs.BaseSystem) {
	s.BaseSystem = bs
}

func (s *{{ .SystemName }}) GetFilter() (ecs.FilterType, []string) {
	return ecs.All, []string{
	}
}
